Start            → begins an instruction block
Return           → ends a command or task
Init             → declares or initializes a variable
if / else        → conditional branches
for / while      → loops
throw / try / catch → exception handling
==               → immutable assignment
=                → mutable assignment
:                → fallback default
;                → end of statement
